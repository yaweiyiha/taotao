<div class="styleguide admin-widget-singledate">
    <div class="prn pln">
  		<div class="input-group" style="width:100%">
			<input class="form-control time" data-key="{{ key }}" placeholder="{{ placeholder }}" :disabled="disable" value="{{time}}" data-valide="{{required ? 'required' : ''}}">
    	</div>

    </div>
</div>